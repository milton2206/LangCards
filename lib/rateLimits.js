// ============================================================================
// Суточные лимиты на пользователя (фаза 7.1). ЕДИНСТВЕННОЕ место с числами —
// поменять лимит можно здесь, и это подхватят и сервер, и подсказки в UI.
// ----------------------------------------------------------------------------
// Зачем. Каждый вызов генерации — это деньги на счёте Claude API и Google TTS,
// которые списываются автоматически. Любой зарегистрировавшийся иначе может
// генерировать бесконечно. Лимит проверяется НА СЕРВЕРЕ (см. lib/serverAuth.js
// и функцию consume_quota в supabase/schema.sql); клиент лимит только показывает.
//
// Модель проста: на каждый вид операции — счётчик за календарные сутки (UTC) и
// минимальный интервал между запросами. Обе величины на пользователя.
//
// Как подобраны числа (запас для реального ученика × потолок для робота/цикла):
//   • cards  40/сут — одна генерация = целая пачка карточек; 40 пачек в день
//     человек не осилит, а сорвавшийся цикл упрётся в потолок. Интервал 4 с:
//     листать карточки всё равно дольше, а скрипт не хлестнёт очередью.
//   • texts  40/сут — текст надо ещё и прочитать; та же логика, что у карточек.
//   • grammar 80/сут — тапов по грамматике за чтение больше, но объяснения
//     кэшируются: к API идёт только НОВОЕ предложение. 80 уникальных разборов в
//     день — с большим запасом. Интервала нет: тапы идут вперемешку с чтением.
//   • tts   300/сут — считается ТОЛЬКО при кэш-промахе (см. lib/tts.js): общий
//     кэш гасит почти все повторы даром. 300 НОВЫХ синтезов на пользователя в
//     день — это очень большой словарь, но бюджет Google ограничен сверху.
//     Интервала нет: фоновый прогрев озвучки идёт частой очередью — это норма.
//   • conjugation 40/сут — таблица спряжения по кнопке на карточке глагола.
//     Кэшируется по слову (клиент), к API идёт только НОВЫЙ глагол; 40 разных
//     глаголов в день — с запасом. Интервала нет: одиночные тапы.
//   • listening 40/сут — подбор похоже звучащих слов, один вызов на подход.
//   • placement 5/сут — наполнение общего банка теста тяжёлое (5 уровней), но
//     редкое и разовое на язык; жёсткий потолок страхует от злоупотребления.
//
// Границы (UTC-сутки, ~одна операция ≈ один вызов модели/озвучки) — трогать
// здесь и только здесь.
// ============================================================================

export const RATE_LIMITS = {
  cards: { perDay: 40, minIntervalSec: 4 },
  texts: { perDay: 40, minIntervalSec: 4 },
  grammar: { perDay: 80, minIntervalSec: 0 },
  conjugation: { perDay: 40, minIntervalSec: 0 },
  listening: { perDay: 40, minIntervalSec: 4 },
  tts: { perDay: 300, minIntervalSec: 0 },
  placement: { perDay: 5, minIntervalSec: 10 },
};
